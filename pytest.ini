[pytest]
; pytest-flask's auto-loaded 'app'/'client' fixtures conflict with our
; own app/client fixtures in tests/conftest.py (pytest-flask expects a
; different app factory contract than create_app() uses). Disabling the
; plugin entirely lets our own fixtures take effect everywhere.
addopts = -p no:flask
